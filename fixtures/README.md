# Fixtures

This directory contains source inputs used to exercise the compiler, CLI, and bake path.

These are not product examples. They may be small, repetitive, or shaped around test coverage rather than app quality.

## Layout

```txt
fixtures/apps/       single-file .flour fixtures
fixtures/projects/   recipe.flour project fixtures
```

Use `examples/` for real runnable apps.
