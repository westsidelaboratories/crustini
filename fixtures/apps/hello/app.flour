+++
crustini = "0.1"
name = "Hello"
fps = 30
window = [128, 64]
+++

app! Main {
  fn setup() {
    clear(Color::Black);
  }

  fn draw() {
    clear(Color::Black);
    text(28, 24, "HELLO WORLD!", Color::White);
  }
}
